version: '3.3'

services:
  sgw:
    container_name: sgw
    restart: always
    image: 'airdb/sgw:dev'
    #command: sleep 300
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080/tcp"
    volumes:
      - ./conf/:/app/conf
